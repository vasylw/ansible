- hosts: all
  become: true
  gather_facts: False
  vars:
    ansible_python_interpreter: "/usr/bin/env python3"
 
  tasks:
 
  - name: Setup an overlay network
      docker_network:
        name: test_overlay
        driver: overlay
        ipam_options:
          subnet: '10.10.10.0/24'
